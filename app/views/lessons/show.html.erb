<h1><%= @lesson.title %></h1>
<h3>Category: <%= @lesson.category.title %></h3>

<%= render partial: "contents/content", locals: {contents: @contents} %>

<% if (@lesson.users.include?(current_user) && privileged) %>
    <%= render partial: "publish_buttons", locals: {lesson: @lesson} %>
<% end %>