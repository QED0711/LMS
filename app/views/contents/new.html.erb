<h2><%= @lesson.title %></h2> 
<h5>Category: <%= @lesson.category.title %></h5>
<% if @contents %>
    <%= render partial: "contents/content", locals: {contents: @contents}%>
<% end %>

<%= render partial: "contents/content_form", locals: {content: Content.new} %>

<%= button_to "Preview", lesson_path(@lesson), method: :get %>
